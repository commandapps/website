'use client';

import CommandApplications from './command-applications';

export default function CommandApplicationsClient({ initialPath }) {
  return <CommandApplications initialPath={initialPath} />;
}

